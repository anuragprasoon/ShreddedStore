import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'iaarkctlrfbumkoaznfk.supabase.co','res.cloudinary.com'],

  },
  reactStrictMode: true,
};

export default nextConfig;
